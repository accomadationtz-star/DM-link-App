// utils/mapPropertyToCard.ts
import { Property } from "../types/property";

export function mapPropertyToCard(property: Property) {
  // Return the full property object for PropertyCard and PropertyDetail to use
  return property;
}
